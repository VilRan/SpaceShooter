<Page
    x:Class="SpaceShooter.Xaml.HighscorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpaceShooter.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="0,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Highscore" Height="75" FontFamily="Showcard Gothic" FontSize="48" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,0" TextAlignment="Center" Grid.Row="0"/>
            <TextBlock TextWrapping="Wrap" Margin="0,0,0,0" Width="500" Grid.Row="1" Text="{x:Bind HighscoreView, Mode=OneWay}"/>
            <Button x:Name="backToMainMenuButton" Content="Back to Main Menu" HorizontalAlignment="Center" VerticalAlignment="Center" Width="175" Margin="0,0,0,0" Click="backToMainMenuButton_Click" Grid.Row="2"/>
        </Grid>
    </Grid>
</Page>
